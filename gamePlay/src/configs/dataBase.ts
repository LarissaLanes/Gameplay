const DATA_BASE_NAME = '@gameplay'

//@ app: coleção
// isso é um padrão para o async storage

const COLLECTION_USER = `${DATA_BASE_NAME}: user`;
const COLLECTION_APPOINTMENT = `${DATA_BASE_NAME}: appointments`;


export {
    COLLECTION_USER,
    COLLECTION_APPOINTMENT
}